// A sanity check for our test runner

describe('Test Runner Sanity Check', function sanityCheck() {
  it('runs this test', function runs(done) {
    console.log('We ran the Test Runner Sanity check!');
    done();
  });
});
